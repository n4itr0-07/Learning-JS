/*
                JS Object Literals

Used to strore keyed collections & complex entities.

property => (key, value) pair

Objects are a collection of properties

*/

let student = {
    name: "Mr Robot",
    age: 23,
    makrs: 95.4,
    city: "localhost"
};

console.log(student);

// Accessing properties
console.log(student.name);
console.log(student.age);