//QS: What is the value of age after the following code runs?

let age = 30;
age + 2; // after 2 years
console.log(age); // 30

//QS: What is the value of avg after this code runs?

let russian = 80;
let english = 90;
let maths = 100;
let avg = (russian + english + maths) / 3;

console.log(avg); // 90
