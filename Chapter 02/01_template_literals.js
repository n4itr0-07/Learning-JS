let a = 5;
let b = 10;

console.log(`Your pay ${a + b}, rupees`);
//console.log("Price is", a+b, "rupees");

// template literals are enclosed by the back-tick (` `)  (grave accent) character instead of double or single quotes. This allows you to embed expressions within the string by wrapping them in ${ }.